1) Need to process - in the start - Done
Assert.assertTrue(validator.isValid("-1.3*5/x=1.2"));

2) x+3=10 - Done
10=x+3

3) Done
Assert.assertTrue(resolver.isSolution("2*x+5+х+5=10", 0));

4)
//Assert.assertFalse(resolver.isSolution("17=2*-6+5", -6));
//Assert.assertFalse(resolver.isSolution("17=2*x+5", -6));

2*-6+5
2/-6+5


5) //2*xx+5+test+5=10

-------------------------------------------
TODO:

additional validation when ()
1) //need to check that we can calculate - done
        //we must think about it as a part of validation
        try {
            calculator.calculate(text, -7);
        }catch ()


2) need to add validator for linear equ x*x=4 - not possible
3) need to trim and toLowerCase()
